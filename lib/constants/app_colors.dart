
import 'package:flutter/material.dart';

abstract final class AppColors {
// Light Theme
static const lightBackground = Color(0xFFF7F7F7);
static const lightSurface = Color(0xFFFFFFFF);
static const lightSurfaceSoft = Color(0xFFF0F0F0);

static const lightPrimary = Color(0xFF111111);
static const lightSecondary = Color(0xFF666666);
static const lightMuted = Color(0xFFA3A3A3);

// Dark Theme
static const darkBackground = Color(0xFF0A0A0A);
static const darkSurface = Color(0xFF141414);
static const darkSurfaceSoft = Color(0xFF1E1E1E);

static const darkPrimary = Color(0xFFF5F5F5);
static const darkSecondary = Color(0xFFA0A0A0);
static const darkMuted = Color(0xFF666666);

// Monochrome Accent
static const accent = Color(0xFF111111);
static const accentLight = Color(0xFF2A2A2A);
static const accentSoft = Color(0xFFE5E5E5);

// Dividers
static const lightDivider = Color(0xFFE5E5E5);
static const darkDivider = Color(0xFF292929);
}

